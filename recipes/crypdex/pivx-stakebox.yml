services:
  portainer:
  postgres:
  multiwallet:
#    data_dir: &data_dir
    chains: pivx zcoin
  pivx:
    walletnotify_cmd: curl -X POST http://multiwallet/pivx/walletnotify/$1
    # rpcuser: crypdex
    # rpcpassword: blackbox
    #  data_dir: /root/data/pivx
