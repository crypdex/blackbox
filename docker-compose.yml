version: "3.7"

services:
  api:
    image: crypdex/blackbox-api:latest
    container_name: api
    restart: "unless-stopped"
    env_file:
      - ./.blackbox.env
      - ./.encryption-key.env
    environment:
      - DATA_DIR=${DATA_DIR}
    volumes:
      - ${DATA_DIR:?DATA_DIR required}:/root/data
    ports:
      - "80:4000"
    depends_on:
      - postgres
    command:
      ["/root/wait-for-it/wait-for-it.sh", "postgres:5432", "--", "./app"]
