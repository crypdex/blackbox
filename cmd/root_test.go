package cmd

import (
	"testing"
)

func TestDefaultServicesDirectory(t *testing.T) {
	// servicesDir, err := NewEnv(nil, false).ServicesDir()
	// assert.NoError(t, err)
	//
	// assert.Equal(t, "/var/lib/blackbox/services", servicesDir)
}
