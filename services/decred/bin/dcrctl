#!/usr/bin/env bash


# wallet related commands are to be run on the wallet daemon (is this necessary?)
if [[ $1 = "--wallet" ]]; then
  cmd="docker exec -it -u decred dcrwallet dcrctl $@"
  echo ${cmd}
  (${cmd})
else
  docker exec -it -u decred dcrd dcrctl $@
fi

